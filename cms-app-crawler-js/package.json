{
  "name": "cms-app-crawler-js",
  "version": "1.0.0",
  "description": "CMS App Playwright test framework — JavaScript",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:verbose": "jest --verbose"
  },
  "dependencies": {
    "@playwright/test": "^1.44.0",
    "playwright": "^1.44.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-playwright-preset": "^4.0.0"
  }
}
