# Target app — full internal base URL (e.g. http://10.0.1.50:8080/myapp/)
APP_URL=

# Login path appended to APP_URL — change this if your login page is not at /login.
# Leave blank to use /login as the default.
# Examples: /app/login   /auth/signin   /j_security_check
APP_LOGIN_PATH=

# App credentials
APP_USERNAME=
APP_PASSWORD=

# CSS selector for any element that ONLY appears after successful login
# Right-click a post-login element → Inspect → copy its id or name as a selector
# Examples: #dashboard   .user-menu   [name='j_idt_header']
APP_SUCCESS_INDICATOR=

# Firecrawl (optional — enhances crawl if set)
FIRECRAWL_API_KEY=

# Anthropic (Planner Service)
ANTHROPIC_API_KEY=

# ── Crawl AI Service — LangChain + Playwright MCP ──────────────────────────
# Change LLM_PROVIDER to switch models without code changes
# Supported: openai | anthropic | azure
LLM_PROVIDER=openai
LLM_MODEL=gpt-4o

# API keys — only the one matching LLM_PROVIDER needs to be set
OPENAI_API_KEY=
AZURE_OPENAI_KEY=
AZURE_OPENAI_ENDPOINT=

# Path to seed Excel file for the crawl AI agent
SEED_EXCEL_PATH=./shared/config/seed.xlsx

# Service URLs (defaults work if all on localhost)
DASHBOARD_URL=http://localhost:8000
CRAWL_URL=http://localhost:8001
GENERATOR_URL=http://localhost:8002
PLANNER_URL=http://localhost:8003
EXECUTOR_URL=http://localhost:8004

# Shared paths
SHARED_DIR=./shared
JAVA_DIR=./shared/java

# Set to false to open a visible browser window during crawl (for debugging)
# Set back to true (or remove) before running in production
CRAWL_HEADLESS=true

# Set to true to flash yellow highlight on each element as agent accesses it
# Only visible when CRAWL_HEADLESS=false
CRAWL_HIGHLIGHT=false

# Browser configuration for Playwright (Phase 2)
# If 'playwright install' is blocked, point at a manually downloaded browser:
#   PLAYWRIGHT_CHROMIUM_PATH=C:\path\to\chrome.exe   (Windows)
#   PLAYWRIGHT_CHROMIUM_PATH=/path/to/chrome          (Mac/Linux)
# Alternatively, use an installed system browser channel:
#   PLAYWRIGHT_CHANNEL=chrome
#   PLAYWRIGHT_CHANNEL=msedge
# Crawl4AI (Phase 1) honors PLAYWRIGHT_BROWSERS_PATH for its own browser cache.
PLAYWRIGHT_CHROMIUM_PATH=
PLAYWRIGHT_CHANNEL=
